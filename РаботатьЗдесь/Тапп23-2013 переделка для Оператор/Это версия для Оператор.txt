Это версия движка Тапп23 от 2013 года для Оператор
- БД Аксцесс
- платформа NET Framework 2.0 x86
- ТОДО: Добавить тут свойства движка

Цель: добавить в Оператор  солюшен Тапп.
Задачи:
- Надо собрать движок для Оператора - только на БД sqlite. 
   - Выбран старый движок, его надо пересобрать под БД sqlite.
     - вместо всей этой системы адаптеров БД один только sqlite просто жестко вшить в проект и все! 
	 + БД как образец лежит в папке документации.
   - Это будет самостоятельный проект, производный от старого проекта движка Тапп.
   - Этот движок нужно внедрить в Оператор как вторую БД.
- надо описать движок (в Readme.txt) - какие его возможности реализованы, что он умеет.

- Документировать возможности Движка в этом релизе - а то неясно, что и как там устроено, как им пользоваться. 
- Добавить новые фичи:
  - это растянет работу надолго - придется переделывать все классы движка.
    Следует сначала получить работающий релиз, и потом уже его прокачивать.
  - Tapp Cell.ServiceValue надо переделать в Double. Так оно больше пригодится.
    - нужна поддержка double в БД, без потери точности!
		- придется изменить БД и адаптер БД.
  - ид ячеек и связей делать 64-битные с ид контейнера.
    - сейчас они 32-битные (MID). Но в БД пишутся как 64-битные.
	- придется добавить новый класс MID из более новых проектов.
	- придется изменить БД и адаптер БД.
	  - они хранятся в БД как Int64 уже.
  - коллекцию ячеек делать со словарями по ид и имени.
    - там могут быть ячейки из других солюшенов? Вряд ли, но попробуем реализовать.
  - коллекцию связей делать со словарями по ид, идфром, идто и еще что потребуется.
    - там могут быть связи из других солюшенов? Вряд ли, но попробуем реализовать. 

Дополнительные цели:
- разработать структуру сущностей для Оператора.
- реализовать интеграцию Оператора и Тапп
  - написать сборку-адаптер для Оператора.
  - написать список команд для работы пользователя с тапп через Оператор.
  - написать сборки команд для работы с Тапп.
  - написать процедуры-конвертеры для импорта данных из внешних форматов в Тапп.
    Чтобы набить тапп данными из внешних источников и использовать эти данные затем.
- разработать GUI-навигатор по сети тапп.	
  	
 