Я мог бы реализовать идентификатор ячейки и связи как Struct а не Class, это бы:
- уменьшило занимаемую ими память,
- не нужны были бы вызовы ID.Copy(), так как структуры автоматически копируются
- не позволит передавать объект по ссылке, если это где-то используется.

Есть идея именовать релизы Тапп фамилиями американских президентов, так, для приколу.